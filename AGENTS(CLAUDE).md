# 协作说明

## 基线

- `网络服务小组_签到看板与月报导出_需求文档_V2.0.docx` 是产品需求基线。
- `docs/requirements.md` 是便于代码评审的需求索引，不替代原始 Word 文档。
- 正式排班、实际签到和月报快照必须保持为不同数据对象。

## 必守业务规则

- 只签到、不签退；班内签到计完整班次分钟数。
- 坐班与维修班独立计薪，禁止跨班去重。
- 同一班同一实际人员、同一席位均最多一条有效签到。
- 临时替班只改变实际签到归属，不回写正式排班。
- 看板和所有报表必须复用同一汇总服务。
- 不增加账号、审批、云同步、SMTP、签退或公平性评价。

## 工程规则

- 渲染进程不得直接访问 Node、SQLite 或文件系统。
- IPC 必须白名单化并校验输入；文件路径必须来自系统选择器或受控数据目录。
- 数据库变更通过 `src/main/database.ts` 中的版本化迁移完成。
- 新业务规则先写领域测试，再接 IPC 和界面。
- 不把 `tests/fixtures/reference-output` 中的样例数据复制到运行时数据目录。

