# 变更日志

## 0.6.4 2026-09-10

- Windows 窗口改为与工作区融合的无边框顶栏，保留原生最小化、最大化和关闭按钮，支持拖拽及双击最大化。
- 顶栏高度收紧为 52px，增加轻量层级标识和渐隐分隔线，交互按钮明确排除在拖拽区之外。
- Windows EXE、任务栏、开始菜单和 NSIS 安装器图标统一使用本地暨南大学矢量校徽。
- 数据库 schema 保持 v4，无需数据迁移，与 v0.6.0–v0.6.3 数据和备份兼容。验收状态见 [v0.6.4 发布验证报告](docs/v0.6.4-release-validation.md)。

## 0.6.3 2026-09-09

- 修复签到页在 1920×1080 下受 1300px 上限影响产生大块空白的问题；人员行、操作菜单及签到按钮在 760×540、950px 和页面缩放下可换行且不截断。
- 设置页新增 7 条班次时段配置，自动计算工时；保存时只同步未开始、无签到历史、仍匹配旧设置且未人工改时的正式班次，冲突时整笔回滚。
- 周末上午默认改为 09:00–12:00；排班解析器以 Excel 明确时段为准，下载模板由 ExcelJS 按当前设置动态生成，随包静态模板同步更新。
- 周班表与日历统一以考勤状态着色，并显示逐人 ✓/×/○/—；签到页恢复结束倒计时、首次定位、折叠结果和轻量打钩反馈。
- 开班满 15 分钟仍有非请假人员未签到时，每班提醒一次；前台显示全窗口动效和内置提示音，后台增加 Windows 通知及任务栏闪烁，记录持久化并定期清理。
- 页面切换、议程、日历事件、导航、弹窗和状态变化使用统一 160–220ms 动效，完整支持 `prefers-reduced-motion`。
- 数据库 schema 保持 v4；班次设置和提醒去重沿用 `settings` 表，保持 v0.6.0 数据与备份向前兼容。验收状态见 [v0.6.3 发布验证报告](docs/v0.6.3-release-validation.md)。

## 0.6.0 2026-09-08

- 改为可折叠侧栏和连续工作区，采用陶土主色 `#A64B36`、暖灰背景及本地暨南大学矢量校徽。侧栏与内容区独立滚动，背景延伸至窗口底部。
- 全站使用 React Aria 控件；人员输入文字与成员 ID 分离，成员编辑使用独立会话，支持未保存内容处理。
- 签到页闭环支持增员、移除、换人、更正、撤销、恢复及补记；批量撤销只撤销本次新增签到。操作历史持久保存，后续变化会阻止冲突逆操作。
- FullCalendar Standard v7 提供七天/日视图、新建、单次编辑、复制、取消、恢复、拖动和拉伸。表单保留分钟精度，有历史签到或已结束的班次锁定普通时间编辑。
- 工时汇总、明细共享筛选上下文；今日记录限定当天，明细每页 50 条。月报文件浏览与勾选分开，预览按需切换。
- 月报草稿按月份串行、带版本保存，切页、切月、正常关闭及更新安装前执行保存协调；保留失败输入并支持重试。旧预览响应不能覆盖新内容，每次导出固定业务与草稿快照。
- 导入改为解析预览、逐项处理关联、应用时复核文件哈希及数据版本；正式 Excel 来源与单次执行调整分开。
- schema v3 增量升级到 v4，旧成员、班次、席位、签到、请假 ID 与历史计薪保留。首次升级前生成并验证独立升级备份；旧备份先在临时副本迁移，失败不替换当前数据库。
- 应用标识、数据目录、更新通道和五类正式输出规则保持兼容。0.5.x 可从更新器升级，0.4.0 需覆盖安装；回退旧版必须恢复升级前备份。
- 打包命令默认 `--publish never`，发布文件经校验后由 GitHub Release 明确上传。验收状态见 [升级验证报告](docs/v0.6.0-upgrade-validation.md)。

## 0.5.1 2026-09-08

- 加班支持预约日期：在「请假与加班」的加班面板可独立选择「加班日期」，预约未来某天的加班，不再与顶部「办理日期」绑定。
- 未来日期的加班保持「待签到」，到点实时签到或事后人工补记后才变「已签到」，并计入加班与总薪酬工时；只对已完成的加班写入月报（工时表、工资考核），计划中的不计入。
- 本次仅 UI 解耦与口径调整，无数据库 schema 变更，无需迁移；0.5.0 数据与备份可向前读取。

## 0.5.0 2026-09-07

- 新增“请假与加班”页面：正式班次可登记、修改和撤销请假，并可选指定代班人。
- 无代班请假从到岗率分母和缺勤中豁免；有代班时由指定代班人签到，签到后锁定请假记录。
- 新增加班安排，自定义同日时段；安排本身不计薪，实时签到或人工补记后才计入加班与总工时。
- 正式班次支持按单次添加办公人员；看板、签到页、工时统计和成员明细同步显示人员角色。
- 工时 XLSX 将加班与普通班次一并写入对应成员明细，时长统一使用 `h`；正式排班 DOCX 不混入加班或临时办公人员。
- 数据库由 schema v2 增量迁移至 v3，保持 0.4.0 manifest v2 备份向前恢复兼容。
- 新增 GitHub Release 更新检查；默认手动，自动模式启动检查并后台下载，安装前仍需用户确认重启。
- Windows 发布现在要求同时上传安装包、`.blockmap` 和 `latest.yml`；安装包仍未配置代码签名。

## 0.4.0 2026-09-06

- 兼容“周六/周日上午、下午分列，人员按第 1–4 周填写”的周末坐班表格。
- 同一排班文件重新导入时会补齐旧版本漏掉的班次，保留已有班次和签到记录。
- 周末分栏排班固定按上午 08:00–12:00（4h）、下午 14:30–17:30（3h）导入，模板同步更新。
- 排班空位不生成签到席位，也不进入到岗率、未到岗和工时统计。
- “排班与成员”新增排班导入模板和人员信息模板下载。
- 随包人员模板已清空个人信息，排班模板已清空负责人单元格。

## 0.3.0 2026-09-06

- 新增独立“设置”入口，集中管理 Windows 登录自启动。
- 默认月报导出目录和备份目录可通过系统目录选择器配置。
- 展示应用数据位置、当前有效排班文件、最近员工文件和随包输出模板。
- 员工 Excel 导入后留存源文件副本，并通过数据库迁移记录来源。
- 新增备份列表、哈希完整性检查、指定备份恢复和外部备份恢复；备份同时包含排班与员工源文件。

## 0.2.0 2026-09-06

- 看板新增 Google Calendar 式连续周时间网格，以绿色、红色和灰色分别显示到岗、未到岗和未到班负责人，并自动定位当前时段。
- 签到首页改为无卡片的当日议程表，显示全部班次、负责人、班次状态和当前班次结束倒计时。
- 新增可开关的 Windows 开机自启动。
- 扩大绩效和工资考核编辑空间，并完成 1100×700、1440×900 窗口验证。
- 工资考核工作量支持编辑，默认采用系统工时并使用 `h` 单位。
- 工时记录改为基于参考 Excel 模板填充；排班 DOCX 直接转换导入的 Excel 表格。

## 0.1.0 2026-09-06

- 建立 Electron、React、TypeScript 与 SQLite 的离线桌面应用骨架。
- 实现排班/成员导入、签到与改错、统一工时汇总、看板、月报草稿和五类文件导出。
- 加入模板资源、参考输出夹具、备份恢复、单元/集成/E2E 测试与 Windows 打包配置。
