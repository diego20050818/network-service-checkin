# 月底导出流程

1. 选择所属年月、填写日期和统计范围，默认上月 26 日到本月 25 日。
2. 统一汇总服务读取有效签到并计算成员工时、迟到和提示。
3. 编辑工作文字、评分、推荐和备注；草稿停止输入后自动保存。
4. 内容预览与文件名预览使用当前草稿和同一数据快照。
5. 选择五类文件及本地输出目录。
6. 主进程创建唯一批次目录，生成文件并重新检查占位符和文件结构。
7. 保存范围、填写信息、工时/评分、模板哈希和文件路径快照。

