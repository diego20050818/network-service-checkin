# ADR 001 只交付桌面应用

状态：已接受

## 决策

首版只创建 `apps/desktop`，Electron 主进程承担本地应用服务，不创建 web、mobile、api 或 worker。

## 原因

需求要求 Win11 单机、断网可用、无需服务器和账号。额外部署单元会增加安装、端口、安全和恢复成本，却不服务首版闭环。

## 后果

渲染进程仍保持浏览器安全边界；若未来批准云同步或多机共享，再以明确接口从主进程服务中拆分后端。

