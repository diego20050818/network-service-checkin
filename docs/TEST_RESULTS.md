# 验证结果

当前候选为 0.6.3（2026-09-09），结果见 [v0.6.3 发布验证报告](v0.6.3-release-validation.md)。下面保留的是 0.5.0 历史记录，不作为新版验收结论。

验证日期：2026-09-07

## 已验证

| 门禁 | 命令或范围 | 结果 |
|---|---|---|
| TypeScript | `npm.cmd run typecheck` | 通过 |
| 领域与服务测试 | `npm.cmd run test` | 15 个测试文件、42 项测试全部通过 |
| 生产构建 | `npm.cmd run build` | 主进程、preload、renderer 均构建成功 |
| Electron E2E | `npm.cmd run test:e2e` | 3 项通过：源码构建版、打包后 EXE、带夹具的日历/签到/报告/设置响应式流程 |
| 生产依赖审计 | `npm.cmd audit --omit=dev` | `electron-updater` 升级至 6.8.9 后为 0 个已知漏洞 |
| 模板随包发布 | `release/win-unpacked/resources/templates` | 8 个 DOCX/XLSX 模板均存在，包含排班和人员导入模板 |
| 窗口适配 | 1100×700、1440×900 截图检查 | 今日议程、周时间网格、请假与加班、绩效评分、工资工作量和设置页均无页面级横向溢出 |
| 周看板状态 | 到岗/未到岗/请假/代班/办公人员/加班夹具 | 正式应到、请假豁免、代班、临时办公人员和加班均按各自口径显示；当前周自动滚到当前时段 |
| 调整规则 | 请假、代班、单次增员和加班服务测试 | 覆盖过去/未来请假、替班签到、重复及签到后限制、加班边界与取消限制、历史人工补记、单次增员与移除限制 |
| 输出参考 | `example` 与导出文件重开检查 | 工时表沿用参考 XLSX 横向成员区块并使用 `h`；已签到/补记加班进入成员明细，正式排班 DOCX 不包含加班或临时办公人员 |
| 数据迁移 | SQLite v2→v3 和重复启动 | 旧班次/席位自动取得正式班、负责人和导入来源默认值；迁移幂等且旧数据不变 |
| 备份兼容 | 0.4.0 结构数据库与 v2 清单恢复测试 | v2 备份清单、哈希校验和源文件恢复格式不变；恢复后自动迁移到 schema v3 并可继续使用 |
| 更新器 | 伪更新适配器与设置页 E2E | 覆盖手动/自动模式、检查、发现、下载进度、失败、已下载和确认安装；自动化不访问真实 GitHub |
| 真实排班回归 | `网络小组排班表_2026.09_周末维修固定版.xlsx` | 识别 118 个工作日/维修班次和 16 个周末坐班班次；周末矩阵未再漏读 |
| 空位与工时 | 解析、签到、看板、报表回归 | 空位不生成席位且不进入分母；周末上午 4h、下午 3h，工资考核默认工作量与工时记录一致 |

构建环境为 Node.js 24.18.0、npm 11.16.0、Electron 44.2.0。Windows x64 NSIS 安装包：

- 文件：`apps/desktop/release/network-service-checkin-0.5.0-x64.exe`
- 大小：119,584,961 字节
- SHA-512（Base64）：`1fkOWBcw4mBAd3qVlUQcZjOmw6UbZMuLt2t59P5HWcVq+LkPHCOoEWp54QMsj8bqsXZ7ZvtDQkriszxOKQXpBQ==`
- 更新元数据：`latest.yml` 的版本、文件名、大小和 SHA-512 均与安装包一致；`.blockmap` 已生成。
- Authenticode：`NotSigned`

## 尚未完成的生产验收

- 尚未在一台无 Node、Python、Office 的干净 Windows 10/11 x64 目标机完成 0.4.0 手动升级、旧备份恢复、后续测试 Release 自动更新、卸载和回滚。
- 冷启动 5 秒、签到 1 秒、30 人月报 15 秒需要在目标硬件和真实数据量下实测。
- 正式分发前需配置组织代码签名证书。
