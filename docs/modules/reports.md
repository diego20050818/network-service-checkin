# 报表模块

报表服务从统一工时快照生成五类文件。工作报表填表人与绩效统计人使用月报填表人；绩效/工资成员行使用对应成员；推荐区使用推荐对象。工资工作量默认采用包含加班的总工时并可覆盖，单位为 `h`。工时记录复用参考 Excel 的成员横向区块，正式班、办公人员和已计薪加班按实际成员一并写入；“签到明细”另标工时类型和人员角色。排班表只转换导入的正式 Excel，不混入请假、加班或临时办公人员。预览是内容预览，不承诺 Word 像素分页一致。
